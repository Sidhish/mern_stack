// let heading = document.getElementById("heading");
// console.dir(heading);
// console.log(heading);

// let headings = document.getElementsByClassName("heading");
// console.dir(headings);
// console.log(headings);

// let para = document.getElementsByTagName("p");
// console.log(para);
// console.dir(para);

//Query selectors for tag
// let firstElements = document.querySelector("p");
// console.dir(firstElements); //returns only the first para (bcz it returns only the first found one)

// let allElements = document.querySelectorAll("p");
// console.dir(allElements); //returns node list (that means it returns all things that have tag name "p")

// Query selectors for class
// let firstElements = document.querySelector(".heading");
// console.dir(firstElements); //returns only the first heading (bcz it returns only the first found one)

// let allElements = document.querySelectorAll(".heading");
// console.dir(allElements); //returns node list (that means it returns all things that have class name "heading")

